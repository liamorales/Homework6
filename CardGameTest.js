var expect = chai.expect;

decscribe (WarGameFunctions , function() {
    describe ('#displayWinner', function () {
        it('winner of the gamme will be displayed', function () {
            let showWinner = (Player1, Player2);
            expect(showWinner).to.equal();
        });
    });
});